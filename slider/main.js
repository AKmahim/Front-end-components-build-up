// ====== slider js ==========

// this is the slider function that we can get from https://kenwheeler.github.io/slick/ 
// we can style the slider as our needed


$('.slider-item').slick({
    // dots: true,
    arrows:false,
    infinite: true,
    speed: 300,
    slidesToShow: 3,
    // centerMode: true,
    // variableWidth: true,
    autoplay: true,
    autoplaySpeed: 2000,

  });